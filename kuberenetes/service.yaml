apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: demonamepsec
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
